{% extends 'base.html.twig' %}

{% block title %}Hello RecrutmentController!{% endblock %}

{% block body %}
    <br>
    <image src="{{ asset('uploads/' ~ e.image) }}" width="175px"/><br>
    <b>Nom:<i>{{ e.nom }}</i></b><br>
    <b>Poste:<i>{{ e.poste }}</i>
    <hr>
    <a href="{{ path('read_equipe') }}" class="btn btn-warning">OK</a>
{% endblock %}